#!/usr/bin/perl
use strict;
use warnings;
use lib `dirname $0 | tr -d '\n'`;
use ScriptScript;

sub main(@){
  runSys qw(sudo update-command-not-found);

  run qw(sudo rm /etc/localtime);
  run qw(sudo ln -s /usr/share/zoneinfo/US/Eastern /etc/localtime);

  run "sudo", "mnt", "--other";
  run "klomp-update";
}

&main(@ARGV);
