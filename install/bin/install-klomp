#!/usr/bin/perl
use strict;
use warnings;
use lib `dirname $0 | tr -d '\n'`;
use ScriptScript;

sub main(@) {
    my $dir = "$ENV{HOME}/.src-cache/klomp";
    installFromDir $dir, "git://github.com/teleshoes/klomp.git";
    cd $dir;
    shell "yes | ./dependency-install.pl";
}

main @ARGV;
