#!/usr/bin/perl
use strict;
use warnings;
use lib `dirname $0 | tr -d '\n'`;
use ScriptScript;

my $HOSTNAME = "wolke-t430s";

sub main(@) {
    getRoot @_;

    open FH, "> /etc/hostname";
    print FH "$HOSTNAME\n";
    close FH;
}

&main(@ARGV);
