#!/usr/bin/perl
use strict;
use warnings;
use ScriptScript;

# Uses $ENV{HOME}

sub main(@) {
    my $bkdir = "$ENV{HOME}.default";
    run "sudo", "rsync", "-ac", "$ENV{HOME}/", $bkdir if not -d $bkdir;
}

main(@_);

