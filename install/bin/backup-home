#!/usr/bin/perl
use strict;
use warnings;
use lib `dirname $0 | tr -d '\n'`;
use ScriptScript;

sub main(@) {
    my $bkdir = "$ENV{HOME}.default";
    run "sudo", "rsync", "-av", "$ENV{HOME}/", $bkdir if not -d $bkdir;
}

main @_;

