#!/usr/bin/perl
use strict;
use warnings;
use lib `dirname $0 | tr -d '\n'`;
use ScriptScript;

sub main(@) {
    installFromDir "$ENV{HOME}/Code/wolke-home-config/utils/thinkpad_acpi";
}

main @ARGV;

