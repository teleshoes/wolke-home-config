#!/usr/bin/perl
use strict;
use warnings;
use ScriptScript;

sub main(@) {
    getRoot @_;

    my fromdir = relToScript "../root-files";
    run "rsync", "-rlptDv", "$fromdir/", '/';
}

main @ARGV;

