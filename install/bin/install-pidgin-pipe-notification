#!/bin/bash
sudo apt-get install libgtk2.0-dev pidgin-dev

DIR="/tmp/pidgin-pipe-notification"
mkdir $DIR
cd $DIR
git clone https://github.com/teleshoes/pidgin-pipe-notification .

make
sudo make install
