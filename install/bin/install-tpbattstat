#!/usr/bin/perl
use strict;
use warnings;
use ScriptScript;

sub main(@) {
    run qw(sudo apt-get install git);
    cd "$ENV{HOME}/Code/tpbattstat-applet";
    run qw(git pull) if -d ".git";
    run "./install.sh"
}

main @ARGV;

