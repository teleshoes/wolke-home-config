#!/usr/bin/perl
use strict;
use warnings;

my $url = 'git://github.com/teleshoes/twip.git';
my $dir = 'twip';
my $installCmd = './install.pl';

system "rm -rf /tmp/$dir";

system "git clone $url /tmp/$dir";

system "cd /tmp/$dir; $installCmd";
