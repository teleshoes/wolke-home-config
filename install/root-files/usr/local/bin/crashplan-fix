#!/usr/bin/perl
use strict;
use warnings;

sub main(@){
  getRoot();
  system "rsync -avP --del /media/stuff/Images/crashplan/electron/ /usr/local/crashplan/electron/";
}

sub getRoot(@){
  if(`whoami` ne "root\n"){
    print "rerunning as root\n";
    exec "sudo", $0, @_;
  }
}

&main(@ARGV);
