##to get a job to show up in fcrondyn dzen printer
## add a hash then two alphanumeric characters to the end of the command
##note that the command IS passed to the shell with that comment
## e.g  0 0 * * * apt-get update #up

##close the window or exit the shell to cancel changes
##merely doing :q will reset the alarms

#0 10 * * 1-5 su wolke -lc 'workalarm' #wk
#@ 4 su wolke -lc 'alarm-once co' #co
#@ 3 su wolke -lc 'alarm-once te' #te

#@ 30 su wolke -lc '/home/wolke/bin/alarm' #al
