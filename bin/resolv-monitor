
sudo resolvchooser $@
if [ $? != 0 ]; then
  exit $?
fi

while [ 1 ]; do
  inotifywait /etc/resolv.conf -e modify;
  date
  echo
  echo "----"
  cat /etc/resolv.conf
  echo "----"
  
  sudo resolvchooser $@
done
