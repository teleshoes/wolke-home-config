#!/usr/bin/perl
use strict;
use warnings;

my $file = "$ENV{HOME}/.fcrontab";

my $usage = "Usage:
  $0 JOBNAME [on|off|toggle]  - comment or uncomment JOBNAME line
     JOBNAME is exactly two alphanumeric characters
     HH:MM is hours 00-23 and minutes 00-59
";

sub setJobState($$);
sub readFcronLines();
sub writeFcronLines($);
sub modifyLine($$);

sub main(@){
  if(@_ == 0){
    listNamedJobs;
    exit 0;
  }
  my $jobName = shift() || '';
  my $arg = shift() || '';

  die $usage if @_ > 0;
  die $usage if $jobName !~ /^[0-9A-Za-z]{2}$/;
  die $usage if $arg !~ /^(on|off|toggle)$/;

  if($arg =~ /^(on|off|toggle)$/){
    setJobState $jobName, $arg;
  }
}

sub setJobState($$){
  my $jobName = shift;
  my $state = shift;

  my $modified = 0;
  my @lines = readFcronLines();
  for my $line(@lines){
    if($line =~ /^\s*(?!##).*#$jobName\s*$/){
      $line = modifyLine $line, $state;
      $modified = 1;
      last;
    }
  }
  if($modified){
    writeFcronLines(\@lines);
    system "fcronreset";
  }else{
    die "Job $jobName not found\n";
  }
}

sub readFcronLines(){
  open FH, "< $file" or die "Could not read $file\n";
  my @lines = <FH>;
  close FH;
  return @lines;
}

sub writeFcronLines($){
  my @lines = @{shift()};
  open FH, "> $file" or die "Could not write $file\n";
  print FH @lines;
  close FH;
}

sub modifyLine($$){
  my $line = shift;
  my $state = shift;
  
  print "OLD: $line";
  my $commented = ($line =~ s/^\s*#([^#])/$1/);
  if($state eq 'on' or ($state eq 'toggle' and not $commented)){
    $line = "#$line";
  }
  print "NEW: $line";
  return $line;
}

&main(@ARGV);
