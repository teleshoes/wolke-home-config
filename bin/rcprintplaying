#!/usr/bin/perl
use strict;
use warnings;

my $arg = shift;
my $rbox = `echo -n \$HOME/.dzen2/printers/rbox`;

while(1){
  my $out = `$rbox`;
  $out =~ s/\^[a-z]+\([^)]*\)//g;
  print $out;
  sleep 1;
}
