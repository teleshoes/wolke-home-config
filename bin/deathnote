#!/usr/bin/perl
use strict;
use warnings;

my $bookmarkFile = `echo -n ~/.deathnote-bookmark`;
my $bookmark;
if(-e $bookmarkFile){
  $bookmark = `cat $bookmarkFile`;
  chomp $bookmark;
  exec "eog \"$bookmark\"";
}

