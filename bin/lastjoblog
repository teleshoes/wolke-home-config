#!/usr/bin/perl
use strict;
use warnings;

my $DIR = '/var/opt/escribe/publisher/log';
if(not -e $DIR){
  print '';
  die 'file not found';
}

my @files = split / |\n/, `ls $DIR`;
if($#files < 0){
  print '';
  die 'file not found';
}

print "$DIR/$files[-1]";

