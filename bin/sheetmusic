#!/usr/bin/perl
use strict;
use warnings;

my $usage = "Usage:
   $0 -h|--help
     Show this message
";

sub main(@){
  if(@_ == 1 and $_[0] =~ /^(-h|--help)$/){
    die $usage;
  }else{
    die $usage;
  }
}
&main(@ARGV);
