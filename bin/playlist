#!/bin/bash

LIST="ls -1"
EDIT=""
while getopts "rRe" OPT; do
    case "$OPT" in
        r | R) LIST="find . -type f ! -name .tmplist";;
        e) EDIT="vim .tmplist";;
    esac
done

$LIST  > .tmplist
$EDIT
mplayer -playlist .tmplist
rm .tmplist
