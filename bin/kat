#!/usr/bin/perl
use strict;
use warnings;

my $usage = "Usage: $0 WORD [WORD ..]\n";

sub run(@);

sub main(@){
  while(@_ > 0 and $_[0] =~ /^-/){
    die $usage;
  }
}

sub run(@){
  print "@_\n";
  system @_;
}

&main(@ARGV);
