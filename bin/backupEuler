#!/bin/bash

EULER_DIR=Desktop/Euler
BAK_DIR=Backup/Euler/`date +%s_%Y-%m-%d`

mkdir -p ~/$BAK_DIR
cp -ar ~/$EULER_DIR/*.hs ~/$BAK_DIR
cp -ar ~/$EULER_DIR/*.txt ~/$BAK_DIR

scp -r ~/$BAK_DIR wolk@`extip`:~/$BAK_DIR
