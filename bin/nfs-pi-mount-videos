#!/usr/bin/perl
use strict;
use warnings;

my $raspiIP = `pi --ip`;
my $remoteDir = "/media/videos";
my $localDir = "/media/pi-videos";

sub main(@){
  system "sudo mkdir -p $localDir";
  system "sudo mount $raspiIP:$remoteDir $localDir";
}

&main(@ARGV);
