#!/bin/bash

for wmafile in *.wma;
do mplayer -vo null -vc dummy -af resample=44100 -ao pcm -ao pcm:waveheader "$wmafile" && lame -m j -h --vbr-new -b 160 audiodump.wav -o "`basename "$wmafile" .wma`.mp3"; done; rm -f audiodump.wav

