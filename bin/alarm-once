#!/usr/bin/perl
use strict;
use warnings;

if((@ARGV != 1 and @ARGV != 2) or $ARGV[0] !~ /^[a-zA-Z0-9][a-zA-Z0-9]$/){
  die "Usage: $0 fcronjob-name {two alphanumeric characters}\n";
}

my $job = shift;
my $arg = shift() || '';

system "alarm $arg";
system "fcronjob $job off";
