module Widgets(
  brightnessW,
  clockW,
  cpuFreqsW,
  cpuScalingCpufreqW,
  cpuScalingCpufreqGovW,
  cpuScalingPstateW,
  cpuScalingSimpleW,
  ekigaW,
  fanW,
  fcrondynW,
  klompW,
  layoutWindowsW,
  micW,
  monitorCpuW,
  monitorMemW,
  netW,
  netStatsW,
  openvpnW,
  paSinkW,
  paSourceW,
  pidginPipeW,
  pingMonitorW,
  progressBarW,
  qtemailW,
  screenSaverW,
  sepW,
  speakerW,
  syncWatchW,
  tpBattStatW,
  thunderbirdW,
  volumeW,
  widthScreenWrapW,
  widthCharWrapW,
  windowTitleW,
  workspaceSwitcherW)where

import Brightness(brightnessW)
import Clock(clockW)
import CpuFreqs(cpuFreqsW)
import CpuScalingCpufreq(cpuScalingCpufreqW, cpuScalingCpufreqGovW)
import CpuScalingPstate(cpuScalingPstateW)
import CpuScalingSimple(cpuScalingSimpleW)
import Ekiga(ekigaW)
import Fan(fanW)
import Fcrondyn(fcrondynW)
import Klomp(klompW)
import LayoutWindows(layoutWindowsW)
import Mic(micW)
import MonitorCpu(monitorCpuW)
import MonitorMem(monitorMemW)
import Net(netW)
import NetStats(netStatsW)
import Openvpn(openvpnW)
import PulseAudioEndpoints(paSinkW,paSourceW)
import PidginPipe(pidginPipeW)
import PingMonitor(pingMonitorW)
import ProgressBar(progressBarW)
import QtEmail(qtemailW)
import ScreenSaver(screenSaverW)
import Sep(sepW)
import Speaker(speakerW)
import SyncWatch(syncWatchW)
import TPBattStat(tpBattStatW)
import Thunderbird(thunderbirdW)
import Volume(volumeW)
import Width(widthScreenWrapW, widthCharWrapW)
import WindowTitle(windowTitleW)
import WorkspaceSwitcher(workspaceSwitcherW)
