#!/usr/bin/perl
use strict;
use warnings;

my $sfSrcDir = `echo -n \$HOME/Desktop/Music/SoundFonts`;
my $sfDestDir = '/usr/share/soundfonts';

$sfSrcDir =~ s/'/'\\''/g;
system "mkdir -p $sfDestDir";
system "cp '$sfSrcDir'/* $sfDestDir";
